{
  "name": "katasumi",
  "version": "1.0.0",
  "description": "Keyboard shortcut search engine",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "keywords": ["shortcuts", "keyboard", "productivity"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
